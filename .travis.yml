language: python
python:
  - "2.7"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y lshw dmidecode python-dateutil python-dmidecode python-lxml usbmount
install:
  - pip install -r device_inventory/requirements.txt
script:
  - sudo python -m unittest discover
